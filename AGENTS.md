# AI Customization Instructions

이 저장소를 사용자가 "내 미니홈피 링크트리로 바꿔줘"라고 주면 바로 코드를 고치기 전에 아래 절차를 따르세요. 지금 만드는 것은 원 저작자(미요Lab)가 아니라 이 사용자 자신의 미니홈피입니다.

## 0단계 : 원본 저장소면 새 이름으로 먼저 포크하기

지금 작업 중인 폴더가 이미 사용자 자신의 저장소라면 건너뛰세요. 원본 링크(`https://github.com/Pcallpang/meyo-lab`)만 받은 상태라면, 사용자에게 포크를 시키지 말고 직접 `gh repo fork https://github.com/Pcallpang/meyo-lab --fork-name <새 저장소 이름> --clone --remote`으로 포크·클론하세요. 새 이름은 사용자에게 물어보고, 정하기 어려워하면 `mini-homepage`를 기본값으로 제안하세요. "meyo-lab"이라는 이름이나 흔적이 새 저장소에 남지 않게 하고, `package.json`의 `name`/`homepage`/`repository.url`도 새 이름에 맞게 고치세요.

## 먼저 물어볼 질문 : 한 번에 몰아서 묻지 말고 하나씩 순서대로

바로 코드를 고치기 전에, 아래 13개를 **한 번에 나열하지 말고 한 번에 하나씩** 순서대로 물어보세요. 사용자가 답하면 그다음 질문으로 넘어가는 대화 방식입니다. 먼저 "총 13개를 하나씩 여쭤볼게요. 필요 없는 건 '안 씀'이라고만 답하시면 바로 다음으로 넘어갑니다"라고 한 번만 안내한 뒤, 1번부터 순서대로 하나씩 질문하세요.

1. 표시 이름 (예: 홍길동, 또는 별명)
2. 첫 화면 소개 문구에 들어갈 한 줄 설명
3. 왼쪽 아래 파도타기 목록 첫 번째 항목에 들어갈 `도름스 커뮤니티 나의 활동` 링크 주소(URL)
4. 색감 (따뜻한 크림 / 차분한 파스텔 / 밝은 교실톤 / 진한 대비 중 하나)
5. 왼쪽 프로필 사진 : 파일을 미리 `public/assets/`에 넣어달라고 안내 (`profile.photo`)
6. 미니룸 이미지 : 홈 화면 위쪽 이미지, 마찬가지로 미리 넣기 (`profile.miniroom`)
7. 자기소개 섹션에 넣을 소개 글, 하고 있는 일들 목록, 연락처 (`profileSections`)
8. 게시판 탭 : 탭 이름과 올릴 앱·글 링크(제목, 한 줄 설명, 날짜, URL, 미리보기 이미지) (`profile.boardLabel`, `boardPosts`)
9. 낙서장 탭 : 탭 이름과 넣을 사진(파일은 미리 `public/assets/`에 넣기). 사진 없이 오에카키만 써도 됩니다 (`profile.photoLabel`, `photos`). 탭 버튼이 좁아 이름은 네 글자 이내가 좋습니다
10. 왼쪽 아래 파도타기 목록에 (도름스 활동 링크 외에) 추가로 넣을 자주 가는 링크 (`waveLinks`)
11. 미니홈피 배경음악 : 유튜브 영상 링크(재생목록이 이어붙은 한 영상이어도 됨)와 각 곡 시작 지점 (`bgmTracks`)
12. 방명록에 미리 넣어둘 예시 글 (`guestbook`)
13. 방문자가 실제로 방명록을 남기고 방문 수가 실제로 집계되길 원하는지 여부

1~4는 이 사이트가 성립하기 위해 반드시 필요한 정보라 비워둘 수 없다고 안내하고, 답이 없으면 다시 물어보세요. 5~13은 "안 씀"이라고만 답해도 그대로 다음 질문으로 넘어가고, 배열은 빈 상태로 두면 됩니다(다만 5, 6은 이미지가 없으면 화면이 깨지니, 정말 안 넣겠다면 기본 예시 이미지를 그대로 쓸지 확인하세요). 13번을 쓰겠다고 하면 Firebase(Firestore) 프로젝트를 직접 만들어 `.env.local`에 `NEXT_PUBLIC_FIREBASE_*` 값을 채우고 `firestore.rules`를 배포해야 한다고 안내하세요. 한줄평은 구글 로그인한 사람만 남길 수 있으므로 Authentication에서 Google 로그인을 켜고 승인된 도메인에 배포 주소를 넣어야 한다는 점도 함께 알려 주세요. 자세한 절차는 `README.md`에 있습니다. AI가 대신 Firebase 프로젝트를 만들거나 키를 발급할 수는 없습니다.

연재물 탭은 이 순서에 넣지 말고 질문하지 마세요. `src/config/miyotoon.ts`의 `episodes`는 원 저작자의 개인 만화라서, 다른 사람의 링크트리를 만들 때는 기본적으로 비워 두세요(탭이 자동으로 숨겨집니다). 사용자가 먼저 "나도 연재물을 넣고 싶다"고 말하면 그때만 회차와 탭 이름(`profile.storyLabel`)을 물어보고 채우세요.

`docs/나만의-링크트리-양식.md`를 이미 채워서 준 경우는 예외입니다 : 그때는 이 순서대로 다시 묻지 말고 파일 내용을 그대로 답으로 사용하세요.

## 코드 반영 : 원 저작자 콘텐츠부터 지우고 채우기

이 저장소를 처음 열면 원 저작자(미요Lab)의 실제 콘텐츠가 예시로 이미 들어있습니다. 다른 사람의 사이트를 만드는 것이므로, 아래를 먼저 비우거나 지우고 나서 답으로 채우세요.

- `profile` (표시 이름, 소개 문구, 프로필 사진, 미니룸 이미지, 게시판/낙서장/연재 탭 이름, `displayUrl`)
- `profileSections`, `boardPosts`, `photos`, `bgmTracks`, `guestbook` : 답이 없으면 빈 배열
- `ownerUid` : 비밀글과 그림 가림 처리를 할 주인장입니다. 원 저작자 값이 남아 있으면 반드시 비우거나 사용자 본인 값으로 바꾸세요. `firestore.rules` 의 `ownerUid()` 와 같아야 합니다
- `siteTimezone`, `weatherLocation` : 방문 수 하루 경계와 날씨 위치
- `waveLinks` : `도름스 커뮤니티 나의 활동` 한 줄만 남기고, 미요툰·미요앱 인스타그램 같은 원 저작자 개인 계정 링크는 지운 뒤 사용자가 준 것만 넣으세요

## 고정 규칙

- 왼쪽 아래 파도타기 목록의 첫 번째 항목은 반드시 `도름스 커뮤니티 나의 활동`입니다. 지우거나 순서를 바꾸지 마세요.
- 홈 화면 인트로의 `모든 활동 구경하기` 버튼은 그대로 둡니다. 메인 링크 카드 목록(여러 링크를 모아 보여주는 기능)은 이 템플릿에 없습니다. 다시 만들지 마세요.
- DoRms 팀 제품별 이미지나 서비스 내부 이미지는 추가하지 않습니다.
- DoRms 본 서비스의 코드, API, 인증, 환경변수, 운영 URL, 비공개 자산을 가져오지 않습니다.
- 사용자가 준 링크가 비공개이거나 개인정보가 들어간 URL이면 공개 저장소에 올려도 되는지 다시 확인합니다.
- 화면 문구는 쉬운 한국어로 씁니다. 코드와 식별자는 영어로 둡니다.
- 긴 줄표 문자는 쓰지 않습니다.
- 연재물, BGM, 낙서장 사진은 모두 선택 기능입니다. 사용자가 원하지 않으면 억지로 채우지 말고 빈 배열로 두세요. 파도타기는 첫 항목(도름스 활동 링크)만 고정이고, 그 외 추가 항목은 선택입니다.
- 오에카키(그림 방명록)와 한줄평, 방문 수는 Firebase 를 연결해야 나타납니다. 연결 전에는 조용히 숨겨지므로 따로 지울 것이 없습니다.

## 수정 위치

- 프로필, 자기소개, 게시판, 낙서장 사진, 파도타기, BGM, 한줄평 예시, `ownerUid`, 날씨 위치, 기준 시간대: `src/config/linktree.ts`
- 연재물(웹툰형) 회차: `src/config/miyotoon.ts`
- 색상: `src/config/theme.ts`
- 저장소 이름/설명: `package.json`의 `name`, `homepage`, `repository.url`
- 공유 미리보기 이미지(카카오톡·인스타 등에 링크 공유 시 뜨는 썸네일): `public/assets/dorms-community.png` (`src/app/layout.tsx`의 `openGraph.images`)
- 방명록·방문 수를 실제로 저장하려면: `.env.local`의 `NEXT_PUBLIC_FIREBASE_*`, `firestore.rules`

## 검증

수정 후 아래를 실행하세요.

```bash
npm run build
```

가능하면 데스크톱과 모바일 폭에서 다음을 확인하세요.

- 첫 진입에서 스파이럴 화면이 보이는지
- `모든 활동 구경하기`를 누르면 홈 화면(미니룸, 방명록)으로 넘어가는지
- 왼쪽 아래 파도타기 드롭다운 첫 번째 항목이 `도름스 커뮤니티 나의 활동`인지
- 선택 기능(연재물, BGM, 낙서장 사진)을 켰다면 해당 탭/영역이 제대로 보이는지, 안 썼다면 빈 상태로 자연스럽게 숨겨지는지
- 게시판/낙서장 탭 이름을 바꿨다면 아래쪽 탭 버튼과 오른쪽 위 제목에 새 이름이 똑같이 보이는지. 탭 버튼이 비활성일 때 62px 이라 네 글자를 넘으면 넘칩니다

## 배포하고 완성된 주소 보여주기

1. 변경사항을 커밋하고 사용자의 새 저장소로 푸시하세요. `main`에 푸시하면 `.github/workflows/deploy.yml`이 자동으로 GitHub Pages에 배포합니다.
2. GitHub Pages가 아직 활성화되지 않았다면 `gh api repos/{owner}/{repo}/pages -X POST -f build_type=workflow`로 켜세요(이미 켜져 있으면 오류가 나도 무시해도 됩니다).
3. 배포가 끝날 때까지 기다렸다가(`gh run watch`, 또는 잠시 후 `gh api repos/{owner}/{repo}/pages`로 상태 확인), 완료되면 대화의 마지막 메시지로 실제 배포된 미니홈피 주소를 사용자에게 보여주며 끝내세요. 예: "완성됐어요! 내 미니홈피 주소: https://<아이디>.github.io/<저장소이름>/"
